{
  "name": "js13k-bootstrap",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp build",
    "start:dev": "node ./server.js",
    "start:dist": "node ./server.js -p",
    "dist": "npm run build && ./compress.sh",
    "build:start:dist": "npm run build && node ./server.js -p"
  },
  "author": "Roberto Serra <js13k@rserra.it>",
  "license": "MIT",
  "devDependencies": {
    "express": "^4.17.1",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-minify": "^3.1.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.1.0",
    "node-sass": "^7.0.0",
    "through2": "^4.0.2"
  },
  "dependencies": {
    "express-compile-sass": "^4.0.0"
  }
}
